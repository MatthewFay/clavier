# Clavier

Clavier is a model that generates sheet music in the style of classical masters.

## Dependency Management
This project uses `pip-tools` for deterministic builds. 
To add a package, add it to `requirements.in`, then run:
`pip-compile requirements.in && pip-sync`
